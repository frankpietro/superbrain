"""Value-bet engine: clustering, similarity, probability, and the bet-agnostic registry.

The algorithm is the product. Phase 4a locks the old ``refactored_src`` behaviour
under a golden regression corpus; improvements in phase 4b must keep it green.
See ``docs/knowledge.md`` → *Algorithm correctness contract*.
"""
